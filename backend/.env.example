# =============================================================
# Order Management System - Environment Configuration
# =============================================================

# 服务器配置
NODE_ENV=development
PORT=6006

# =============================================================
# 安全配置 (生产环境必须修改!)
# =============================================================

# JWT 密钥 - 生产环境必须设置32位以上强随机字符串
# 生成方法: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-in-production-must-be-32-chars

# Session 密钥 (如使用session)
SESSION_SECRET=your-session-secret-change-in-production-must-be-32-chars

# =============================================================
# 数据库配置
# =============================================================

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=your-database-password
DB_DATABASE=order_management

# 连接池配置 (生产环境)
DB_POOL_MAX=20
DB_POOL_MIN=5

# SSL 连接 (云数据库通常需要)
DB_SSL=false

# =============================================================
# Redis 配置 (队列和缓存)
# =============================================================

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# =============================================================
# CORS 配置
# =============================================================

# 允许的前端域名 (多个用逗号分隔)
# 生产环境示例: https://www.example.com,https://admin.example.com
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# 允许的 API 连接源 (用于 CSP 配置)
ALLOWED_API_ORIGINS=http://localhost:6006

# 兼容旧配置
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# =============================================================
# 短信服务配置 (阿里云)
# =============================================================

SMS_ACCESS_KEY_ID=
SMS_ACCESS_KEY_SECRET=
SMS_SIGN_NAME=
SMS_TEMPLATE_CODE=

# =============================================================
# 文件上传配置
# =============================================================

UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760

# =============================================================
# 日志配置
# =============================================================

# 日志目录
LOG_DIR=./logs

# 日志级别: error, warn, log, debug, verbose
LOG_LEVEL=log
